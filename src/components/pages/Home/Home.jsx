import * as React from "react"
import { Container, Row, Button } from "reactstrap"

const Home = (props) => {
  const {  } = props

  return (
    <Container fluid>
     Holaaaaaaaaaaa
    </Container>
  )
}

export default Home